# WhatsApp Bot 🤖

Автоматизированный бот для WhatsApp с веб-интерфейсом управления.

## 📋 Описание

WhatsApp Bot предназначен для автоматической обработки сообщений в WhatsApp. Включает веб-панель для удобного управления и запуска бота.

## 🚀 Возможности

- ✅ Веб-интерфейс для управления ботом
- ✅ Автоматическая обработка возражений клиентов
- ✅ Интеграция с базой данных
- ✅ Анализ ответов с помощью AI
- ✅ Автоматическое форматирование сообщений
- ✅ Логирование всех действий

## 📦 Технологии

- **Backend**: Node.js, Express
- **Frontend**: Vanilla JS, HTML5, CSS3
- **WhatsApp**: Green API (только для отправки/получения сообщений)
- **AI**: OpenAI GPT-4o (прямое API подключение)
- **Database**: MySQL
- **Proxy**: SOCKS5 (socks-proxy-agent) - обход региональных ограничений
- **Process Manager**: PM2
- **Web Server**: Nginx

## 🔧 Установка локально

### Предварительные требования

- Node.js (v18.x или новее)
- MySQL
- Git
- OpenAI API ключ
- Green API аккаунт

### Шаги установки

1. **Клонирование репозитория:**
```bash
git clone https://github.com/City-Mars/bot.git
cd bot
```

2. **Установка зависимостей:**
```bash
npm install
```

3. **Настройка переменных окружения:**
```bash
cp .env.example .env
# Отредактируйте .env и добавьте свои настройки
```

4. **Запуск сервера:**
```bash
npm start
```

5. **Открытие в браузере:**
```
http://localhost:3000
```

## 🌐 Развертывание на сервере

Подробная инструкция по развертыванию на production-сервере находится в файле [deploy.md](deploy.md).

### Быстрое развертывание

**Параметры:**
- Домен: `capitalwabot.com`
- Путь: `/www/WhatsAppBot/`
- Порт: `3000`

**Основные шаги:**

1. Установка Node.js и PM2
2. Загрузка проекта на сервер
3. Настройка Nginx
4. Получение SSL сертификата
5. Запуск через PM2

Полная инструкция: [deploy.md](deploy.md)

## 📁 Структура проекта

```
WhatsAppBot/
├── server.js                 # Главный веб-сервер Express
├── botRunner.js              # Запуск и управление ботом (основная логика)
├── objectionHandler.js       # Обработка возражений через RAG + OpenAI
├── responseAnalyzer.js       # Анализ ответов клиентов через OpenAI
├── databaseConnect.js        # Подключение к БД MySQL
├── formatterName.js          # Форматирование имен
├── proxyConfig.js            # Конфигурация SOCKS5 прокси для OpenAI
├── testObjectionHandler.js   # Тестирование обработки возражений
├── index.html                # Веб-интерфейс управления ботом
├── nginx.conf                # Конфигурация Nginx
├── ecosystem.config.cjs      # Конфигурация PM2
└── package.json              # Зависимости проекта
```

### 🔍 Описание ключевых модулей

- **server.js** - HTTP сервер с API endpoints для управления ботом
- **botRunner.js** - Основная логика бота: диалоги, состояния, обработка сообщений WhatsApp
- **objectionHandler.js** - Определение и обработка возражений через OpenAI RAG (File Search)
- **responseAnalyzer.js** - Анализ sentiment ответов клиентов (позитивный/негативный/нейтральный)
- **proxyConfig.js** - Централизованная настройка SOCKS5 прокси для всех OpenAI запросов

## 🎯 Использование

1. Откройте веб-интерфейс
2. Введите номер телефона (например: `79991234567`)
3. Укажите ID объекта
4. Нажмите "Запустить бота"
5. Отсканируйте QR-код в консоли (при первом запуске)

## 🔐 Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
# Сервер
PORT=3000
NODE_ENV=production

# Green API Settings
ID_INSTANCE=your_instance_id
API_TOKEN_INSTANCE=your_api_token

# OpenAI Settings
OPENAI_API_KEY=your_openai_api_key

# Vector Store (опционально, для обработки возражений через RAG)
VECTOR_STORE_ID=your_vector_store_id

# SOCKS5 Proxy Settings (опционально, для обхода региональных ограничений OpenAI)
PROXY_HOST=45.81.35.40
PROXY_PORT=1080
USE_PROXY=false  # Отключить прокси (если прокси недоступен или не нужен)

# База данных
DB_HOST=localhost
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=whatsappbot
```

### 🌐 Настройка прокси

Если вам нужно использовать SOCKS5 прокси для доступа к OpenAI API (например, для обхода региональных ограничений), добавьте в файл `.env`:

```env
PROXY_HOST=45.81.35.40
PROXY_PORT=1080
USE_PROXY=true  # или просто не указывайте эту переменную
```

**Отключение прокси** (если прокси недоступен или не нужен):
```env
USE_PROXY=false
```

Прокси будет автоматически использоваться для всех запросов к OpenAI API. Если прокси не настроен или отключен, бот будет использовать прямое подключение.

**💡 Совет:** Если вы видите ошибку `Connection error` при запуске тестов, попробуйте отключить прокси: `USE_PROXY=false`

См. также: `QUICK_FIX.md` и `PROXY_TROUBLESHOOTING.md`

## 📊 API Endpoints

- `POST /api/start-bot` - Запуск бота
- `POST /api/stop-bot` - Остановка бота
- `GET /api/status` - Статус бота

## 🐛 Устранение неполадок

### Бот не запускается

```bash
# Проверка логов
npx pm2 logs whatsappbot

# Перезапуск
npx pm2 restart whatsappbot
```

### Проблемы с WhatsApp

- Проверьте настройки Green API в `.env`
- Убедитесь, что ID_INSTANCE и API_TOKEN_INSTANCE правильные

### Проблемы с OpenAI API

- Проверьте, что OPENAI_API_KEY правильный
- Если есть региональные ограничения, настройте SOCKS5 прокси
- Проверьте логи на наличие ошибок прокси-соединения

### Ошибки базы данных

- Проверьте настройки в `.env`
- Убедитесь, что MySQL запущен

## 📝 Логирование

Логи доступны через PM2:

```bash
# Все логи
pm2 logs whatsappbot

# Только ошибки
pm2 logs whatsappbot --err

# Последние 100 строк
pm2 logs whatsappbot --lines 100
```

## 🔄 Обновление

```bash
# Получение последних изменений
git pull origin main

# Установка новых зависимостей
npm install

# Перезапуск
pm2 restart whatsappbot
```

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменений (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект является частной разработкой.

## 🙏 Благодарности

- [Green API](https://green-api.com/) - WhatsApp Business API (для отправки/получения сообщений)
- [OpenAI](https://openai.com/) - GPT-4o API (прямое подключение)
- [Express](https://expressjs.com/) - Web framework
- [PM2](https://pm2.keymetrics.io/) - Process manager
- [socks-proxy-agent](https://github.com/TooTallNate/node-socks-proxy-agent) - SOCKS5 Proxy для обхода региональных ограничений

---

**Разработано с ❤️ для автоматизации работы с клиентами**

